import React, { useContext } from 'react';
import { DiaryContext } from '../../App';

const Home = () => {
  const { data } = useContext(DiaryContext);

  return <div></div>;
};

export default Home;
